#!/bin/bash

emacs_args=()
console=true

for arg in "$@"; do
    emacs_args+=("$arg")
    [ "$arg" = '-c' ] && console=false
done

$console && emacs_args=(-nw "${emacs_args[@]}")

exec emacsclient -s "${HOME}/.emacs.d/sockets/server" "${emacs_args[@]}"
