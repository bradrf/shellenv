# -*- Mode: shell-script -*-

export AWS_CONFIG_FILE="${HOME}/creds/aws-brad.conf"

# eval `awk -F= '{ if ($2) print "export " toupper($1) "=" $2}' "$AWS_CONFIG_FILE"`

# if [ -z "$JAVA_HOME" ]; then
#     if [ -e '/usr/libexec/java_home' ]; then
#         export JAVA_HOME="$(/usr/libexec/java_home)"
#     else
#         jb="$(which java 2>/dev/null)"
#         if [ -z "$jb" ]; then
#             echo "${BASH_SOURCE}: Unable to locate java to set JAVA_HOME" 1>&2
#         else
#             export JAVA_HOME="$(dirname "$(dirname "$jb")")"
#         fi
#     fi
# fi
# 
# AWS_CREDENTIALS_CSV="${HOME}/creds/brad-access-key.csv"
# if [ -f "$AWS_CREDENTIALS_CSV" ]; then
#     eval `awk -F, 'END {print "export AWS_USER_NAME="$1";export AWS_ACCESS_KEY="$2";export AWS_SECRET_KEY="$3}' "$AWS_CREDENTIALS_CSV"`
# fi
# 
# AWS_BASEDIR='/opt/aws'
# 
# for d in `\find /opt/aws -maxdepth 2 -type d -name bin`; do
#     echo "$PATH" | grep -qE ":${d}(:|\$)" || export PATH="${PATH}:$d"
#     dbase="$(dirname "$d")"
#     case `basename "$dbase"` in
#         ec2-api-tools*)
#             export EC2_HOME="$dbase"
#             ;;
#     esac
# done

# vi: set filetype=sh
