[user]
	name = bradrf
	email = brad@gigglewax.com
[core]
	excludesfile = /Users/brad/.gitignore_global
	autocrlf = input
[color]
	ui = true
[alias]
	co = checkout
	st = status
	br = branch
	brl = branch -a --no-merged
	bra = branch -r --list
        br-clean = !sh -c 'git branch --merged | grep -v "^\\*" | xargs -n1 git branch -d'
        br-recent = for-each-ref --sort=-committerdate --format='%(committerdate) %(authorname) %(refname:short)' refs/heads/
        br-find = branch --contains
        stl = stash list
        std = stash show -u
        sdiff = diff --staged
        wdiff = diff --color-words
        lg = log --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr %an)%Creset' --abbrev-commit --date=relative
        # can optionally pass "--all" or a branch name to tree:
        #tree = log --graph --decorate --oneline
	tree = !git lg --graph
	unstage = reset HEAD
	last = log -1 HEAD
	visual = !gitk
	st = status
	uncommit = reset --soft HEAD^
	progress = diff --stat
	pg = diff --shortstat
[Push]
	default = matching
